# tuples cannot be modified to add or remove items.

names = ("Ace", "Brand", "Colly")

# all operations are the same as lists

# to sort a tuple
print(sorted(names))

# create a new tuple
new_names =names + ("Ryan", "Kel")
print(new_names)